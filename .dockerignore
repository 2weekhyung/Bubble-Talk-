# 빌드 컨텍스트에서 제외할 항목들
.gradle
out
.idea
*.log
.DS_Store
Dockerfile
docker-compose.yml

# 데이터 폴더 제외 (권한 에러 방지 및 빌드 속도 향상)
mysql_data/

# 빌드 폴더 설정
# build 폴더 전체를 무시하되, libs 내의 jar 파일만 예외적으로 포함시킴
build/
!build/libs/*.jar
